{
	"name": "pi-experiments",
	"version": "0.0.0",
	"private": true,
	"description": "Dev workspace for public pi extensions. Each directory under extensions/ is its own independently packaged, installable extension.",
	"type": "module",
	"scripts": {
		"lint": "node scripts/lint.mjs",
		"test": "node --experimental-strip-types --test extensions/*/tests/*.test.ts"
	},
	"devDependencies": {
		"@earendil-works/pi-ai": "0.82.1",
		"@earendil-works/pi-coding-agent": "0.82.1",
		"@earendil-works/pi-tui": "0.82.1",
		"typebox": "1.3.8"
	}
}
